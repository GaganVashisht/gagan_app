class User {
  constructor(userId, name, activeFrom, role, status) {
    this.userId = userId;
    this.name = name;
    this.activeFrom = activeFrom;
    this.role = role;
    this.status = status;
  }
}
export default User;
